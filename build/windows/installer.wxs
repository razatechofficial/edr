<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">
  <Product Id="*"
           Name="EDR Agent"
           Language="1033"
           Version="$(var.Version)"
           Manufacturer="RazaTech"
           UpgradeCode="7D3A6EE5-52D7-4D6A-9D66-6AFC9FA04A90">

    <Package InstallerVersion="500" Compressed="yes"
             InstallScope="perMachine"
             InstallPrivileges="elevated"/>

    <MajorUpgrade DowngradeErrorMessage=
      "A newer version is already installed."/>
    <MediaTemplate EmbedCab="yes"/>

    <Feature Id="ProductFeature" Title="EDR Agent" Level="1">
      <ComponentGroupRef Id="ProductComponents"/>
    </Feature>
  </Product>

  <Fragment>
    <Directory Id="TARGETDIR" Name="SourceDir">
      <Directory Id="ProgramFiles64Folder">
        <Directory Id="INSTALLFOLDER" Name="EDR Agent"/>
      </Directory>
    </Directory>
  </Fragment>

  <Fragment>
    <!-- ServiceInstall must share a Component with the service exe (File KeyPath).
         Config lives in the same component so #[ConfigFile] binds reliably for light.exe. -->
    <ComponentGroup Id="ProductComponents"
                    Directory="INSTALLFOLDER">
      <Component Id="AgentExe" Guid="*">
        <File Id="AgentExe"
              Source="dist\windows-amd64\edr-agent.exe"
              KeyPath="yes"/>
        <File Id="ConfigFile"
              Source="build\windows\config.yml"
              Name="config.yml"/>
        <ServiceInstall
          Id="EDRAgentService"
          Name="EDRAgent"
          DisplayName="EDR Agent"
          Description="Endpoint Detection and Response Agent"
          Type="ownProcess"
          Start="auto"
          ErrorControl="normal"
          Arguments='--config "[#ConfigFile]"'/>
        <ServiceControl
          Id="StartService"
          Start="install"
          Stop="both"
          Remove="uninstall"
          Name="EDRAgent"
          Wait="yes"/>
      </Component>
    </ComponentGroup>
  </Fragment>
</Wix>
